# 재사용성 가이드

## 🎯 핵심 원칙: 패턴 일관성이 최우선

> "새로운 것보다 일관된 것이 낫다"

### 우선순위
1. **기존 패턴 검색** → 2. **동일 패턴 적용** → 3. **새 표준 정립**

---

## 📋 패턴 검색 체크리스트

### 1. API 패턴
```bash
# 어떤 도구를 사용하는가?
grep -r "fetch\|axios\|XMLHttpRequest" src/ | head -10

# 어떻게 호출하는가?
grep -r "async.*function.*api" src/ | head -10

# 에러는 어떻게 처리하는가?
grep -r "catch\|handleError" src/ | head -10
```

**발견 예시:**
- fetch 사용 → fetch만 사용
- axios 사용 → axios만 사용
- 커스텀 클라이언트 → 동일한 클라이언트 사용

### 2. 상태 관리 패턴
```bash
# 로컬 상태 관리
grep -r "useState\|useReducer" src/ | head -10

# 전역 상태 관리
grep -r "Context\|zustand\|redux\|mobx" src/ | head -10
```

**발견 예시:**
- useState만 사용 → 복잡한 상태도 useState로
- zustand 사용 → 모든 전역 상태는 zustand로
- Context API → 새로운 Context도 동일 패턴

### 3. 폼 처리 패턴
```bash
# 폼 라이브러리
grep -r "react-hook-form\|formik" src/ | head -10

# 검증 방식
grep -r "yup\|zod\|validate" src/ | head -10
```

**발견 예시:**
- 수동 처리 → 계속 수동으로
- react-hook-form → 모든 폼에 적용
- zod 검증 → 모든 검증에 zod

### 4. 스타일링 패턴
```bash
# CSS 방식
grep -r "styled-components\|emotion\|css modules\|tailwind" src/ | head -10
```

**발견 예시:**
- CSS Modules → 계속 CSS Modules
- Tailwind → 모든 스타일링 Tailwind로
- styled-components → 동일하게 적용

### 5. 테스트 패턴
```bash
# 테스트 도구
grep -r "jest\|vitest\|testing-library" src/ | head -10

# 테스트 구조
grep -r "describe\|it\|test" src/__tests__ | head -10
```

---

## 🔍 패턴 분석 방법

### Step 1: 유사 기능 찾기
```bash
# 예: 새로운 API 호출 구현 시
find src -name "*.api.ts" -o -name "*api*.ts" | xargs grep -l "export"
```

### Step 2: 패턴 추출
```typescript
// 기존 패턴 예시
async function getUser(id: string) {
  try {
    const response = await fetch(`/api/users/${id}`);
    if (!response.ok) throw new Error('Failed');
    return await response.json();
  } catch (error) {
    console.error(error);
    throw error;
  }
}
```

### Step 3: 동일하게 적용
```typescript
// ✅ 좋은 예 - 기존 패턴과 일치
async function getOrder(id: string) {
  try {
    const response = await fetch(`/api/orders/${id}`);
    if (!response.ok) throw new Error('Failed');
    return await response.json();
  } catch (error) {
    console.error(error);
    throw error;
  }
}

// ❌ 나쁜 예 - 다른 패턴 도입
async function getOrder(id: string) {
  return axios.get(`/api/orders/${id}`)  // 갑자기 axios?
    .then(res => res.data)
    .catch(err => {
      logger.error(err);  // 다른 로깅 방식?
      throw err;
    });
}
```

---

## 📐 새 패턴 정립 시

### 기존 패턴이 없을 때만!

1. **팀 협의 필수**
   ```markdown
   ## 새 패턴 제안: WebSocket 연결

   기존 패턴: 없음
   제안 방식: socket.io 사용
   이유: 가장 안정적이고 문서화가 잘 됨
   ```

2. **첫 구현이 표준**
   ```typescript
   // 이 구현이 향후 WebSocket의 표준이 됨
   export function createWebSocket(url: string) {
     // ... 구현
   }
   ```

3. **문서화 필수**
   ```markdown
   ## WebSocket 패턴 (2025-01-06 정립)

   사용 라이브러리: socket.io
   연결 방식: createWebSocket 함수
   에러 처리: 자동 재연결 3회
   ```

---

## ⚠️ 안티패턴 (하지 말아야 할 것)

### ❌ "더 나은" 방법 도입
```typescript
// 프로젝트가 Promise를 쓰는데...
// ❌ 나쁜 예
async function betterWay() {  // async/await가 "더 좋으니까"
  const data = await fetch();
}

// ✅ 좋은 예
function consistentWay() {  // 기존 패턴 유지
  return fetch().then(data => {});
}
```

### ❌ 부분적 마이그레이션
```typescript
// ❌ 나쁜 예 - 일부만 새 방식
// 파일 A: fetch 사용
// 파일 B: axios 사용 (새로 도입)
// → 일관성 깨짐!
```

### ❌ 개인 취향 반영
```typescript
// ❌ 나쁜 예
// 기존: handleClick
// 새 코드: onClickHandler  // "내가 좋아하는 스타일"
```

---

## 🚀 실전 예시

### 시나리오 1: 새 API 엔드포인트 추가
```bash
# 1. 기존 API 패턴 확인
$ grep -r "api" src/features/user/
src/features/user/api/getUser.ts: export async function getUser()

# 2. 패턴 분석
$ cat src/features/user/api/getUser.ts
# fetch 사용, try-catch 패턴 확인

# 3. 동일하게 적용
# src/features/order/api/getOrder.ts 생성
# 완전히 동일한 구조로 작성
```

### 시나리오 2: 새 폼 컴포넌트
```bash
# 1. 기존 폼 패턴 확인
$ find src -name "*Form.tsx" | head -3
src/features/login/ui/LoginForm.tsx

# 2. 패턴 분석
$ grep "handleSubmit\|onChange" src/features/login/ui/LoginForm.tsx
# 수동 상태 관리, onChange 핸들러 패턴 확인

# 3. 동일하게 적용
# OrderForm도 똑같은 방식으로 구현
```

---

## 📊 재사용성 메트릭

### 측정 지표
- **패턴 일관성**: 동일 기능의 구현 방식 통일도
- **코드 중복률**: 유사 코드 비율
- **재사용 횟수**: 모듈/컴포넌트 사용 빈도

### 목표
- 패턴 일관성: 95% 이상
- 코드 중복률: 5% 이하
- 재사용 횟수: 평균 3회 이상

---

## 🎓 학습 자료

### 기존 패턴 학습
1. `src/features/` 디렉토리의 첫 3개 feature 분석
2. 각 레이어별 대표 파일 1개씩 읽기
3. 테스트 파일에서 패턴 확인

### 체크리스트
- [ ] API 호출 패턴 파악 완료
- [ ] 상태 관리 패턴 파악 완료
- [ ] 폼 처리 패턴 파악 완료
- [ ] 스타일링 패턴 파악 완료
- [ ] 테스트 패턴 파악 완료

---

## 💡 팁

1. **의심스러우면 기존 코드 확인**
   - 비슷한 기능 3개 이상 확인
   - 가장 많이 사용된 패턴 선택

2. **PR 리뷰 시**
   - "기존 패턴과 다른데 이유가 있나요?"
   - "이 부분은 XXX 파일의 패턴과 맞추면 어떨까요?"

3. **새 프로젝트 시작 시**
   - 첫 주는 패턴 파악에 집중
   - 패턴 문서 작성 후 개발 시작

---

## 📝 패턴 문서화 템플릿

새 패턴을 정립했다면:

```markdown
## [기능명] 패턴

**정립일**: YYYY-MM-DD
**정립자**: @username
**승인자**: @team-lead

### 사용 도구
- 라이브러리: XXX
- 버전: X.X.X

### 패턴 구조
\```typescript
// 코드 예시
\```

### 사용 예시
\```typescript
// 실제 사용 예
\```

### 주의사항
- 이 패턴을 벗어나면 안 되는 이유
- 예외 상황 처리 방법
```

---

## 🔄 마이그레이션 정책

### 원칙: 전체 마이그레이션만 허용

**금지**: 부분적 마이그레이션
**허용**: 전체 코드베이스 일괄 변경

### 절차
1. RFC 작성 및 팀 동의
2. 마이그레이션 브랜치 생성
3. 전체 변경 후 일괄 머지
4. 새 패턴 문서화

---

**Remember**: 일관성 > 최적화 > 최신 기술