# Commit Command Configuration
# Smart Commit with Conventional Commits

extends: "_base.yaml"

# ============================================================================
# COMMAND INFO
# ============================================================================

command: "commit"
type: "utility"
description: "스마트 커밋 - Conventional Commits 형식 자동 생성"

# ============================================================================
# PREREQUISITES
# ============================================================================

prerequisites:
  - git

# ============================================================================
# CONVENTIONAL COMMITS
# ============================================================================

conventional_commits:
  enabled: true
  format: "<type>(<scope>): <subject>"

  types:
    - name: "feat"
      description: "새로운 기능 추가"

    - name: "fix"
      description: "버그 수정"

    - name: "docs"
      description: "문서 업데이트"

    - name: "style"
      description: "코드 포맷팅"

    - name: "refactor"
      description: "리팩터링"

    - name: "test"
      description: "테스트 추가/수정"

    - name: "chore"
      description: "기타 변경사항"

# ============================================================================
# FEATURE ID DETECTION
# ============================================================================

feature_id_detection:
  enabled: true
  script: ".claude/lib/git/detect-feature-id.sh"
  auto_include_in_message: true

# ============================================================================
# HOOKS
# ============================================================================

hooks:
  pre_execution:
    - script: ".claude/hooks/pre-commit"
      description: "Pre-commit 검증"

# ============================================================================
# AUTO FOOTER
# ============================================================================

footer:
  enabled: true
  template: |

    🤖 Generated with [Claude Code](https://claude.com/claude-code)

    Co-Authored-By: Claude <noreply@anthropic.com>
