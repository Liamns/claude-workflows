# 📋 Notion Start - Notion 기능 명세서 기반 작업 시작

⚠️ **CRITICAL: 이 지시사항을 절대 건너뛰지 마세요**

**Claude를 위한 필수 실행 순서 (예외 없음):**

아래 단계를 **정확히 이 순서대로** 따라야 합니다.

1. **Notion 검색**: bash 라이브러리 함수를 사용하여 Notion 데이터베이스 검색
2. **선택 질문 (AskUserQuestion)**: 검색 결과를 AskUserQuestion으로 변환하여 사용자에게 제시
3. **사용자 선택 대기**: 사용자가 기능을 선택할 때까지 대기 (절대 자동 진행 금지)
4. **템플릿 파싱**: 선택된 Notion 페이지의 템플릿 내용 파싱 (기능 목적, 비즈니스 로직, 체크리스트, 연관 기능)
5. **업데이트 확인 (AskUserQuestion)**: Notion 페이지를 업데이트할지 사용자에게 확인 요청
6. **Notion 업데이트**: 사용자가 확인한 경우에만 시작일, 담당자, 진행현황을 "개발중"으로 업데이트
7. **/triage 자동 호출**: 파싱된 템플릿 내용을 기반으로 /triage 워크플로우 실행
8. **세션 정보 저장**: 현재 작업 중인 Notion 페이지 정보 캐시에 저장

**절대 금지 사항:**
- ❌ Notion 검색 단계를 건너뛰지 마세요
- ❌ AskUserQuestion 없이 자동으로 첫 번째 검색 결과를 선택하지 마세요
- ❌ 사용자 선택 없이 템플릿 파싱을 진행하지 마세요
- ❌ 사용자 확인 없이 Notion 페이지를 업데이트하지 마세요
- ❌ Notion 업데이트 실패 시에도 /triage를 호출하지 마세요

---

## 📋 다음 단계 추천 시 필수 규칙

### Notion 검색 결과 제시 시 AskUserQuestion 사용

Notion 검색 완료 후, **사용자에게 작업할 기능을 선택하도록 요청할 때** 반드시 AskUserQuestion 도구를 사용하세요.

**❌ 잘못된 예시:**
```
"3개의 기능을 찾았습니다. 어떤 기능을 선택하시겠습니까?"
```

**✅ 올바른 예시:**
```
"Notion 검색 완료: 3개의 기능을 찾았습니다."

[AskUserQuestion 호출]
- question: "어떤 기능을 작업하시겠습니까?"
- header: "Feature"
- options: [
    {label: "회원 가입 기능", description: "P0 | 대기"},
    {label: "로그인 기능", description: "P0 | 대기"},
    {label: "비밀번호 찾기", description: "P1 | 대기"}
  ]
```

### 사용자 선택 후 자동 진행

**사용자가 기능을 선택하면 즉시 다음 단계로 진행하세요:**

```bash
{"0": "회원 가입 기능"}  → 템플릿 파싱 시작
{"0": "로그인 기능"}      → 템플릿 파싱 시작
```

---

## Overview

Notion 기능 명세서에서 작업을 시작하는 통합 워크플로우입니다.
Notion 데이터베이스를 검색하고, 선택한 기능의 템플릿을 파싱하여, 자동으로 워크플로우를 시작합니다.

## Output Language

**IMPORTANT**: 사용자가 확인하는 모든 출력은 반드시 **한글로 작성**해야 합니다.

**한글 작성 대상:**
- Notion 검색 결과 안내
- 템플릿 파싱 진행 상황
- Notion 업데이트 완료 메시지
- /triage 호출 안내
- 에러 메시지 및 경고

**영어 유지:**
- 명령어 (/notion-start, /triage)
- 파일 경로
- 함수 이름

이 커맨드는 다음을 수행합니다:
1. **Notion 검색**: 키워드로 Notion 기능 명세서 데이터베이스 검색
2. **대화형 선택**: AskUserQuestion으로 작업할 기능 선택
3. **템플릿 파싱**: 선택된 기능의 상세 내용 파싱
4. **작업 시작**: Notion 페이지 속성 업데이트 (시작일, 담당자, 진행현황)
5. **워크플로우 연동**: /triage를 자동 호출하여 최적 워크플로우 시작

**주요 기능:**
- Notion 기능 명세서 통합 검색
- 우선순위, 그룹별 필터링 지원
- 템플릿 구조 자동 파싱 (🎯 기능 목적, 🔄 비즈니스 로직, 📋 체크리스트, 🔗 연관 기능)
- KST 타임존 기반 날짜 관리
- /triage 워크플로우 자동 연동

## Usage

```bash
# 기본 검색 (app 데이터 소스)
/notion-start "회원 가입"

# 검색어 없이 실행 (전체 대기 중인 기능 표시)
/notion-start

# 우선순위 필터 (향후 지원 예정)
/notion-start "인증" --priority=P0

# 그룹 필터 (향후 지원 예정)
/notion-start --group="인증"
```

---

## 🔄 Workflow Steps

### Step 1: Notion 검색

**목적**: Notion 기능 명세서 데이터베이스에서 작업할 기능 검색

**실행 방법**:
```bash
# Bash 라이브러리 로드
source .claude/lib/notion-search.sh

# 검색 실행 (app 데이터 소스 기본)
search_results=$(search_by_keyword "$keyword" "app")

# 검색 결과 없을 경우 안내
if [[ -z "$search_results" ]] || [[ "$search_results" == "[]" ]]; then
    echo "검색 결과가 없습니다. 다른 키워드로 다시 시도해주세요."
    exit 1
fi
```

**예상 출력**:
```json
[
  {
    "id": "page-id-123",
    "properties": {
      "기능 설명": {"title": [{"plain_text": "회원 가입 기능"}]},
      "우선순위": {"select": {"name": "P0"}},
      "진행현황": {"status": {"name": "대기"}}
    }
  }
]
```

---

### Step 2: 선택 질문 (AskUserQuestion)

**목적**: 검색 결과를 사용자에게 제시하고 선택 받기

**실행 방법**:
```bash
# 검색 결과를 AskUserQuestion 형식으로 변환
question_json=$(convert_search_to_question "$search_results" "어떤 기능을 작업하시겠습니까?" "Feature")

# AskUserQuestion 도구 호출
# Claude는 이 JSON을 AskUserQuestion 도구의 questions 파라미터로 전달
```

**AskUserQuestion 형식**:
```json
[
  {
    "question": "어떤 기능을 작업하시겠습니까?",
    "header": "Feature",
    "multiSelect": false,
    "options": [
      {
        "label": "회원 가입 기능",
        "description": "P0 | 대기"
      },
      {
        "label": "로그인 기능",
        "description": "P0 | 대기"
      }
    ]
  }
]
```

**사용자 응답 처리**:
```javascript
// 사용자가 "회원 가입 기능" 선택 시
{"0": "회원 가입 기능"}

// 선택된 기능의 page_id 추출
// search_results에서 label과 매칭되는 항목의 id 사용
```

---

### Step 3: 템플릿 파싱

**목적**: 선택된 Notion 페이지의 템플릿 내용 파싱

**실행 방법**:
```bash
# Bash 라이브러리 로드
source .claude/lib/notion-parser.sh

# 템플릿 파싱
template_data=$(parse_notion_template "$page_id")

# 파싱 결과 출력 예시
echo "📋 기능 명세 파싱 완료:"
echo ""
echo "🎯 기능 목적:"
echo "$template_data" | jq -r '.sections.purpose'
echo ""
echo "🔄 비즈니스 로직:"
echo "$template_data" | jq -r '.sections.logic'
echo ""
echo "📋 체크리스트:"
echo "$template_data" | jq -r '.sections.checklist'
echo ""
echo "🔗 연관 기능:"
echo "$template_data" | jq -r '.sections.related'
```

**예상 출력**:
```json
{
  "metadata": {
    "title": "회원 가입 기능",
    "priority": "P0",
    "group": "인증",
    "status": "대기",
    "assignee": "",
    "start_date": "",
    "deadline": ""
  },
  "sections": {
    "purpose": "신규 사용자가 서비스에 가입할 수 있도록 합니다...",
    "logic": "1. 이메일 중복 확인\n2. 비밀번호 해싱...",
    "checklist": "- [ ] 이메일 유효성 검증\n- [ ] 비밀번호 강도 검사...",
    "related": "- 로그인 기능\n- 비밀번호 찾기..."
  },
  "raw_content": "# 회원 가입 기능\n\n## 🎯 기능 목적..."
}
```

---

### Step 4: 프로젝트 환경 감지

**목적**: 현재 프로젝트의 기술 스택 및 환경 파악

**실행 방법**:
```bash
# package.json 확인
project_type="unknown"
if [[ -f "package.json" ]]; then
    # React, Next.js, Capacitor 등 감지
    if grep -q "react" package.json; then
        project_type="react"
    fi
    if grep -q "@capacitor/core" package.json; then
        project_type="capacitor"
    fi
fi

# 추가 환경 정보
# - TypeScript 사용 여부 (tsconfig.json)
# - 프레임워크 (package.json의 dependencies)
# - 빌드 도구 (vite.config, webpack.config 등)
```

**활용**:
- /triage 호출 시 프로젝트 컨텍스트 전달
- Capacitor 프로젝트인 경우 플랫폼별 고려사항 포함

---

### Step 5: 업데이트 확인 (AskUserQuestion)

**목적**: Notion 페이지를 실제로 업데이트할지 사용자에게 확인

**실행 방법**:
```bash
# AskUserQuestion 도구 호출
# Claude는 다음 정보를 포함한 확인 질문 제시:
# - 업데이트할 페이지 제목
# - 시작일 (오늘 날짜)
# - 진행현황 변경 (대기 → 개발중)
# - 담당자 정보
```

**AskUserQuestion 형식**:
```json
{
  "questions": [
    {
      "question": "Notion 페이지를 실제로 업데이트하시겠습니까? (시작일: 2025-11-20, 진행현황: 개발중, 담당자: 박진원)",
      "header": "Update",
      "multiSelect": false,
      "options": [
        {
          "label": "예, 업데이트 진행",
          "description": "실제 Notion 페이지를 업데이트하고 작업 시작"
        },
        {
          "label": "아니오, 세션만 저장",
          "description": "업데이트 없이 세션 정보만 저장"
        }
      ]
    }
  ]
}
```

**사용자 응답 처리**:
- **"예, 업데이트 진행" 선택 시**: Step 6 (Notion 업데이트) 진행
- **"아니오, 세션만 저장" 선택 시**: Step 6 건너뛰고 Step 8 (세션 정보 저장)으로 이동

**중요**: 사용자가 "아니오"를 선택한 경우, /triage 호출도 건너뛰어야 합니다.

---

### Step 6: Notion 업데이트

**목적**: 사용자가 확인한 경우에만 작업 시작을 Notion에 기록 (시작일, 담당자, 진행현황)

**실행 방법**:
```bash
# Bash 라이브러리 로드
source .claude/lib/notion-update.sh

# Notion 페이지 업데이트
if ! update_notion_start "$page_id"; then
    echo "❌ Notion 업데이트 실패. 작업을 중단합니다."
    exit 1
fi

echo "✅ Notion 페이지 업데이트 완료"
echo "   - 시작일: $(get_kst_date)"
echo "   - 진행현황: 개발중"
echo "   - 담당자: $(get_current_user_id)"
```

**업데이트 내용**:
- **시작일**: 현재 KST 날짜 (YYYY-MM-DD)
- **진행현황**: "개발중"
- **담당자**: 현재 Notion MCP 사용자

---

### Step 7: /triage 호출

**목적**: 파싱된 템플릿 내용을 기반으로 최적 워크플로우 자동 시작

**실행 방법**:
```bash
# 템플릿 내용을 /triage에 전달
# SlashCommand 도구 사용
SlashCommand("/triage", template_description)
```

**template_description 구성**:
```markdown
# 기능: {title}

## 기능 목적
{sections.purpose}

## 비즈니스 로직
{sections.logic}

## 체크리스트
{sections.checklist}

## 연관 기능
{sections.related}

## 메타데이터
- 우선순위: {priority}
- 기능 그룹: {group}
```

**/triage 자동 실행**:
- 복잡도 분석 수행
- 워크플로우 추천 (Epic/Major/Minor/Micro)
- AskUserQuestion으로 워크플로우 선택
- 선택된 워크플로우 자동 시작

---

### Step 8: 세션 정보 저장

**목적**: 현재 작업 중인 Notion 페이지 정보를 캐시에 저장

**실행 방법**:
```bash
# 캐시 디렉토리 확인
cache_dir=".claude/cache"
mkdir -p "$cache_dir"

# 1. 현재 Notion 페이지 ID 저장
echo "$page_id" > "${cache_dir}/current-notion-page.txt"

# 2. 워크플로우 컨텍스트 저장 (JSON)
cat > "${cache_dir}/current-workflow-context.json" <<EOF
{
  "page_id": "$page_id",
  "title": "$(echo "$template_data" | jq -r '.metadata.title')",
  "priority": "$(echo "$template_data" | jq -r '.metadata.priority')",
  "group": "$(echo "$template_data" | jq -r '.metadata.group')",
  "start_date": "$(get_kst_date)",
  "workflow": "notion-start",
  "timestamp": "$(date -u +"%Y-%m-%dT%H:%M:%SZ")"
}
EOF

echo "✅ 세션 정보 저장 완료"
echo "   - Page ID: $page_id"
echo "   - Context: ${cache_dir}/current-workflow-context.json"
```

**저장되는 정보**:
- `current-notion-page.txt`: Notion 페이지 ID (단순 텍스트)
- `current-workflow-context.json`: 전체 워크플로우 컨텍스트 (JSON)

**활용**:
- 작업 중 언제든지 현재 Notion 페이지 참조 가능
- /commit 시 작업내역 자동 기록에 활용 (Phase 3)
- 워크플로우 재개 시 컨텍스트 복원

---

## Error Handling

### Notion 검색 실패
```bash
if [[ -z "$search_results" ]]; then
    echo "❌ Notion 검색 실패"
    echo "다음을 확인해주세요:"
    echo "  1. Notion MCP 서버 연결 상태"
    echo "  2. .claude/config/notion.json 설정"
    echo "  3. 검색 키워드"
    exit 1
fi
```

### 템플릿 파싱 실패
```bash
if ! template_data=$(parse_notion_template "$page_id"); then
    echo "❌ 템플릿 파싱 실패"
    echo "Notion 페이지에 다음 섹션이 있는지 확인해주세요:"
    echo "  - 🎯 기능 목적"
    echo "  - 🔄 비즈니스 로직"
    echo "  - 📋 체크리스트"
    echo "  - 🔗 연관 기능"
    exit 1
fi
```

### Notion 업데이트 실패
```bash
if ! update_notion_start "$page_id"; then
    echo "❌ Notion 업데이트 실패"
    echo "다음을 확인해주세요:"
    echo "  1. Notion 페이지 권한"
    echo "  2. MCP 서버 연결 상태"
    echo "  3. 네트워크 연결"
    echo ""
    echo "⚠️  작업을 계속하시겠습니까?"
    # 사용자 확인 후 진행 여부 결정
fi
```

---

## Examples

### Example 1: 기본 검색

```bash
/notion-start "회원 가입"
```

**실행 과정**:
1. Notion 검색: "회원 가입" 키워드로 검색
2. 검색 결과: 2개 기능 발견
3. AskUserQuestion: "어떤 기능을 작업하시겠습니까?"
   - 회원 가입 기능 (P0 | 대기)
   - 회원 가입 API (P1 | 대기)
4. 사용자 선택: "회원 가입 기능"
5. 템플릿 파싱: 기능 목적, 비즈니스 로직, 체크리스트, 연관 기능 추출
6. AskUserQuestion: "Notion 페이지를 실제로 업데이트하시겠습니까?"
7. 사용자 확인: "예, 업데이트 진행"
8. Notion 업데이트: 시작일 2025-11-20, 진행현황 "개발중"
9. /triage 호출: 복잡도 분석 및 워크플로우 추천
10. 세션 저장: `.claude/cache/current-notion-page.txt` 생성

### Example 2: 검색어 없이 실행

```bash
/notion-start
```

**실행 과정**:
1. Notion 검색: 빈 키워드로 검색 (전체 "대기" 상태 기능 표시)
2. 검색 결과: 4개 기능 발견 (AskUserQuestion 최대 개수)
3. AskUserQuestion: "어떤 기능을 작업하시겠습니까?"
4. 이후 동일

---

## Integration with /triage

`/notion-start`는 `/triage`와 완벽하게 통합됩니다:

1. **템플릿 내용 전달**: Notion 템플릿 파싱 결과를 /triage에 전달
2. **복잡도 분석**: /triage가 기능 목적, 비즈니스 로직, 체크리스트를 분석
3. **워크플로우 추천**: Major/Minor/Micro 중 최적 워크플로우 제안
4. **자동 실행**: 사용자 선택 시 해당 워크플로우 즉시 시작

**전달되는 정보**:
- 기능 제목 (title)
- 기능 목적 (purpose)
- 비즈니스 로직 (logic)
- 체크리스트 (checklist)
- 연관 기능 (related)
- 메타데이터 (priority, group)

**/triage 호출 시 템플릿**:
```markdown
다음 기능 개발을 시작합니다:

# {title}

## 기능 목적
{sections.purpose}

## 비즈니스 로직
{sections.logic}

## 체크리스트
{sections.checklist}

## 연관 기능
{sections.related}

## 메타데이터
- 우선순위: {metadata.priority}
- 기능 그룹: {metadata.group}
- Notion Page: {page_id}

복잡도를 분석하고 최적 워크플로우를 추천해주세요.
```

---

## Best Practices

### 1. 명확한 검색 키워드 사용
- ✅ "회원 가입", "로그인", "주문 생성"
- ❌ "기능", "구현", "개발"

### 2. Notion 템플릿 구조 준수
필수 섹션이 모두 포함되어야 합니다:
- 🎯 기능 목적
- 🔄 비즈니스 로직
- 📋 체크리스트
- 🔗 연관 기능

### 3. 작업 시작 전 Notion 확인
- 기능 우선순위 확인
- 선행 작업 완료 여부 확인
- 연관 기능 영향도 파악

### 4. /triage 워크플로우 활용
- 복잡도 분석 결과 참고
- 추천된 워크플로우 선택
- Plan Mode 활용 (Major 이상)

---

## Troubleshooting

### Q: Notion 검색이 실패합니다
**A**: 다음을 확인하세요:
1. `.claude/config/notion.json` 파일이 존재하는지
2. `database.data_sources.app.url` 값이 올바른지
3. Notion MCP 서버가 실행 중인지

### Q: 템플릿 파싱 시 섹션을 찾을 수 없습니다
**A**: Notion 페이지에 다음 형식의 헤더가 있는지 확인하세요:
```markdown
## 🎯 기능 목적
## 🔄 비즈니스 로직
## 📋 체크리스트
## 🔗 연관 기능
```

### Q: Notion 업데이트가 실패합니다
**A**: Notion 페이지 권한을 확인하세요. MCP 사용자가 편집 권한이 있어야 합니다.

### Q: /triage가 자동 호출되지 않습니다
**A**: Notion 업데이트가 성공했는지 확인하세요. 업데이트 실패 시 /triage는 호출되지 않습니다.

---

## Version History

- **v1.0.0** (2025-11-20): 초기 버전
  - Notion 검색, 템플릿 파싱, 업데이트 통합
  - /triage 워크플로우 연동
  - 세션 정보 저장

---

## Related Commands

- `/triage`: 복잡도 분석 및 워크플로우 추천
- `/major`: 신규 기능 구현 (Major 워크플로우)
- `/minor`: 기능 개선 (Minor 워크플로우)
- `/micro`: 버그 수정 (Micro 워크플로우)
- `/commit`: 변경사항 커밋

---

## Technical Details

### Bash Libraries Used
- `.claude/lib/notion-search.sh`: Notion 검색 기능
- `.claude/lib/notion-parser.sh`: 템플릿 파싱 기능
- `.claude/lib/notion-update.sh`: Notion 업데이트 기능
- `.claude/lib/notion-date-utils.sh`: KST 날짜 유틸리티
- `.claude/lib/notion-config.sh`: Notion 설정 관리

### MCP Tools Used
- `mcp__notion-company__notion-search`: Notion 검색
- `mcp__notion-company__notion-fetch`: 페이지 가져오기
- `mcp__notion-company__notion-update-page`: 페이지 업데이트
- `mcp__notion-company__notion-get-self`: 현재 사용자 정보

### Cache Files
- `.claude/cache/current-notion-page.txt`: 현재 작업 중인 Notion 페이지 ID
- `.claude/cache/current-workflow-context.json`: 워크플로우 컨텍스트 정보
