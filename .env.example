# Claude Workflow Environment Configuration
# Copy this file to .env and adjust values as needed

# Environment mode (development or production)
# - development: Auto-approve document generation (faster iteration)
# - production: Request user consent for each document
export CLAUDE_ENV="development"  # or "production"

# Usage:
# - Development mode: For testing and rapid prototyping
# - Production mode: For actual feature implementation with review

# How to use:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env and set CLAUDE_ENV to your preferred mode
# 3. Source the file: source .env
# 4. Run your workflow: /major "Your feature name"

# Examples:
# export CLAUDE_ENV="development"  # Auto-approve all documents (fast)
# export CLAUDE_ENV="production"   # Manual review for each document (safe)
