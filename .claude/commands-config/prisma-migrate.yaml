# Prisma-Migrate Command Configuration
# Prisma 마이그레이션 도구

extends: "_base.yaml"

# ============================================================================
# COMMAND INFO
# ============================================================================

command: "prisma-migrate"
type: "utility"
description: "Prisma 마이그레이션 생성 및 적용"

# ============================================================================
# PREREQUISITES
# ============================================================================

prerequisites:
  - prisma
  - node

# ============================================================================
# SCRIPT
# ============================================================================

script:
  path: ".claude/lib/prisma-migrate.sh"
  description: "Prisma 마이그레이션 스크립트 (완전히 구현됨)"
  status: "implemented"

# ============================================================================
# DOCUMENTATION
# ============================================================================

documentation:
  status: "완성"
  file: ".claude/commands/prisma-migrate.md"
  completeness: "100%"

# ============================================================================
# SUPPORTING SCRIPTS
# ============================================================================

supporting_scripts:
  - path: ".claude/lib/db-utils.sh"
    description: "데이터베이스 유틸리티"
