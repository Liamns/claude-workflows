# Claude Workflows - Project Context

> v2.4.0 - 통합 워크플로우 & 에이전트 최적화 버전

## 🎯 프로젝트 개요

Claude Workflows는 Claude Code에서 개발 생산성을 극대화하는 토큰 효율적 워크플로우 자동화 시스템입니다.

### 핵심 가치
- **토큰 효율성**: 작업 복잡도에 따라 60-85% 토큰 절감
- **개발 속도**: 3배 이상 생산성 향상
- **품질 보장**: 모든 단계에서 자동 검증

## 📂 프로젝트 구조

```
claude-workflow/
├── .claude/
│   ├── commands/           # 슬래시 명령어 (10개)
│   │   ├── triage.md       # 워크플로우 자동 선택
│   │   ├── major.md        # 통합 Major 워크플로우
│   │   ├── minor.md        # 버그 수정 워크플로우
│   │   ├── micro.md        # 간단한 변경
│   │   ├── test.md         # 테스트 자동화
│   │   ├── commit.md       # 스마트 커밋
│   │   ├── pr-review.md    # PR 자동 리뷰
│   │   └── review.md       # 종합 코드 리뷰
│   │
│   ├── agents/            # 통합 에이전트 (6개)
│   │   ├── architect-unified.md    # 아키텍처 검증
│   │   ├── reviewer-unified.md     # 종합 코드 리뷰
│   │   ├── implementer-unified.md  # TDD 구현
│   │   ├── documenter-unified.md   # 문서화 & 커밋
│   │   └── _deprecated/            # 구 버전 에이전트
│   │
│   ├── skills/            # 자동 활성화 스킬 (13개)
│   ├── docs/              # 프로젝트 문서
│   ├── architectures/     # 아키텍처 패턴 지원
│   └── workflow-gates.json # 품질 게이트 설정
│
├── .specify/              # spec-kit 통합
│   ├── memory/
│   │   └── constitution.md  # 프로젝트 원칙
│   └── config/
│       └── architecture.json # 아키텍처 설정
│
├── README.md              # 간소화된 메인 문서 (104줄)
├── CHANGELOG.md           # 버전 히스토리
└── install.sh            # 자동 설치 스크립트
```

## 🚀 주요 기능

### 1. 통합 워크플로우 시스템 (v2.4.0)

#### /major (통합 명령어)
- 6개 명령어 → 1개로 통합
- 10개 질문 → 2개로 축소 (80% 감소)
- 자동 저장/재개 기능

#### /minor
- 75% 토큰 절감
- 빠른 버그 수정
- 기존 패턴 유지

#### /micro
- 85% 토큰 절감
- 간단한 변경사항
- 즉시 실행

### 2. 통합 에이전트 (v2.4.0)

#### architect-unified
- **통합**: architect + fsd-architect
- **지원**: FSD, Clean, Hexagonal, DDD 등 12개 패턴
- **모델**: sonnet

#### reviewer-unified
- **통합**: code-reviewer + security-scanner + impact-analyzer
- **검토**: 보안(OWASP Top 10), 성능, 품질, 영향도
- **모델**: sonnet

#### implementer-unified
- **통합**: quick-fixer + test-guardian
- **방식**: TDD 우선, 최소 변경 원칙
- **모델**: sonnet

#### documenter-unified
- **통합**: changelog-writer + smart-committer
- **기능**: Conventional Commits, Notion 연동
- **모델**: haiku

### 3. 핵심 명령어

```bash
# 워크플로우 자동 선택
/triage "사용자 인증 기능 추가"

# 통합 Major 워크플로우
/major

# 테스트 자동화
/test

# 코드 리뷰
/review src/
/review --adv  # 심층 분석

# Git 자동화
/commit
/pr-review 123
```

## 🔧 Quick Start

### 1. 설치
```bash
curl -fsSL https://raw.githubusercontent.com/Liamns/claude-workflows/main/install.sh | bash
```

### 2. 프로젝트 설정
```bash
/start  # 아키텍처 선택 및 초기 설정
```

### 3. 작업 시작
```bash
/triage "버그 수정: 로그인 에러"  # AI가 최적 워크플로우 선택
```

## 📊 성능 지표

### v2.4.0 개선사항
- **명령어**: 16개 → 10개 (38% 감소)
- **에이전트**: 11개 → 6개 (45% 감소)
- **질문 수**: 평균 10개 → 2개 (80% 감소)
- **README**: 395줄 → 104줄 (74% 감소)

### 토큰 효율성
| 워크플로우 | 토큰 절감 | 적합 작업 |
|-----------|---------|----------|
| Major | 60% | 새 기능, 복잡한 구현 |
| Minor | 75% | 버그 수정, 개선 |
| Micro | 85% | 간단한 변경 |

## 🏗 아키텍처 지원

### 구현 완료 (6개)
- **Frontend**: FSD, Atomic Design
- **Backend**: Clean Architecture, Hexagonal, DDD, Layered

### 계획 중 (8개)
- MVC, Micro-frontend, Serverless, Monorepo 등

## ⚡ 스킬 시스템 (13개)

자동으로 활성화되는 패턴:
- `bug-fix-pattern`: 일반적인 버그 자동 수정
- `api-integration`: API 통합 패턴
- `form-validation`: 폼 검증 로직
- `platform-detection`: 플랫폼별 조건부 렌더링
- `mobile-build`: Android/iOS 빌드 자동화
- 기타 8개 스킬

## 🔄 Breaking Changes (v2.4.0)

### 명령어 변경
- `major-*` 개별 명령어 → `/major` 통합 명령어
- 예: `major-specify`, `major-plan` 등 → `/major`로 통합

### 에이전트 이름 변경
- `architect` → `architect-unified`
- `code-reviewer` → `reviewer-unified`
- `quick-fixer` → `implementer-unified`
- `smart-committer` → `documenter-unified`

### 파일 경로 변경
- 루트의 `agents/`, `skills/` 폴더 제거
- 모든 파일은 `.claude/` 내부로 이동

## 💡 사용 팁

### 1. 작업 복잡도 판단 불확실할 때
```bash
/triage "작업 설명"  # AI가 자동으로 최적 워크플로우 선택
```

### 2. 테스트 우선 개발
```bash
/test  # 테스트 작성 → 구현 → 검증 자동화
```

### 3. 코드 품질 유지
```bash
/review --adv  # 보안, 성능, 영향도 종합 분석
```

## 📚 추가 문서

- `SUB-AGENTS-GUIDE.md`: 에이전트 상세 가이드
- `SKILLS-GUIDE.md`: 스킬 시스템 문서
- `CHANGELOG.md`: 버전별 변경사항

## 🔗 관련 링크

- GitHub: https://github.com/Liamns/claude-workflows
- 이슈 리포트: https://github.com/Liamns/claude-workflows/issues

---

*이 문서는 새로운 Claude 세션에서 프로젝트를 빠르게 파악하기 위한 핵심 정보를 담고 있습니다.*
*상세한 내용은 각 가이드 문서를 참조하세요.*