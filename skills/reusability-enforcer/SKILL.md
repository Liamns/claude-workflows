---
name: reusability-enforcer
description: 코드 작성 전 기존 재사용 가능 모듈을 자동 검색하고 제안합니다. Article X (재사용성 우선 원칙)를 강제하여 중복 코드를 방지하고 패턴 일관성을 유지합니다.
allowed-tools: Read, Grep, Glob, mcp__context7*
context7_enabled: true
context7_auto_activate:
  - feature_type: "new_component"
  - stage: "pre-implementation"
  - complexity_score: ">5"
context7_loading:
  max_tokens: 2000
  priority_components:
    - shared/ui
    - shared/lib
    - shared/hooks
    - entities/*/ui
    - features/*/model
  filters:
    - "reusable components"
    - "utility functions"
    - "custom hooks"
---

# Reusability Enforcer Skill

Article X (재사용성 우선 원칙)를 실행하여 기존 코드 재사용을 극대화합니다.

## 실행 조건

다음 상황에서 자동 활성화:
- `/triage` 명령 실행 시
- Major/Minor 워크플로우 시작
- 새 컴포넌트/기능 생성 요청
- "재사용 가능한 코드 찾아줘" 요청

## 재사용성 검색 프로세스

### Step 1: Context7 가용성 확인

Context7이 사용 가능하고 활성화 조건을 만족하면:

```yaml
Context7 로드 전략:
1. 프로젝트 구조 파악 (500 토큰)
   - src/ 디렉토리 전체 구조
   - 아키텍처 패턴 파악

2. 유사 컴포넌트 검색 (1000 토큰)
   - shared/ui/* 컴포넌트
   - entities/*/ui/* 패턴
   - features/*/ui/* 구현

3. 유틸리티 함수 수집 (500 토큰)
   - shared/lib/* 함수
   - shared/hooks/* 커스텀 훅
   - shared/api/* API 클라이언트
```

🔍 **Context7 활성화 메시지**:
```
🔍 Context7 활성화: 재사용 가능 모듈 검색 중...
  ├─ 프로젝트 구조 로드 (500/2000 토큰)
  ├─ 유사 컴포넌트 검색 (1000/2000 토큰)
  └─ 유틸리티 함수 수집 (500/2000 토큰)
```

### Step 2: 수동 검색 (Context7 불가 시)

Context7이 사용 불가능하거나 토큰 한도 초과 시:

```bash
# 1. 컴포넌트 검색
glob "shared/ui/**/*.tsx"
glob "entities/*/ui/*.tsx"
glob "features/*/ui/*.tsx"

# 2. 패턴 매칭
grep -r "export.*function.*${componentName}" shared/
grep -r "export.*const.*${hookName}" shared/hooks/

# 3. 후보 파일 읽기
read shared/ui/Button/Button.tsx
read shared/hooks/useModal.ts
```

### Step 3: 재사용 가능성 평가

발견된 모듈을 평가하여 재사용 적합도 점수 계산:

```yaml
평가 기준:
  기능 일치도: 40%
  인터페이스 호환성: 30%
  의존성 적합성: 20%
  성능 요구사항: 10%

점수 분류:
  90-100점: 완벽 일치 - 즉시 재사용
  70-89점: 높은 일치 - 약간 수정 후 재사용
  50-69점: 중간 일치 - 래핑하여 재사용
  30-49점: 낮은 일치 - 참고용
  0-29점: 재사용 부적합 - 새로 생성
```

### Step 4: 재사용 제안 생성

```markdown
## 🔄 재사용 가능 모듈 발견!

### 1. Button 컴포넌트 (95% 일치)
- 위치: `shared/ui/Button/Button.tsx`
- 사용법: `import { Button } from '@/shared/ui';`
- 수정 필요: variant prop 추가만 필요

### 2. useModal 훅 (85% 일치)
- 위치: `shared/hooks/useModal.ts`
- 사용법: `import { useModal } from '@/shared/hooks';`
- 수정 필요: onClose 콜백 추가

### 3. httpClient (70% 일치)
- 위치: `shared/api/httpClient.ts`
- 사용법: `import { httpClient } from '@/shared/api';`
- 수정 필요: 에러 핸들링 커스터마이징

### 📊 재사용 통계
- 예상 코드 절감: 350줄
- 개발 시간 절약: 2시간
- 테스트 커버리지: 이미 95% 달성
```

## 패턴 일관성 강제

### 기존 패턴 감지

```typescript
// 프로젝트에서 발견된 패턴
const patterns = {
  stateManagement: 'zustand',
  dataFetching: 'react-query',
  formHandling: 'react-hook-form',
  styling: 'tailwind',
  componentStructure: 'FSD'
};
```

### 패턴 준수 가이드

```markdown
⚠️ 패턴 일관성 알림

현재 프로젝트는 다음 패턴을 사용 중입니다:
- 상태 관리: Zustand (Redux 대신)
- 데이터 페칭: React Query (fetch 직접 사용 금지)
- 폼 처리: React Hook Form + Zod

새로운 "더 나은" 방법 대신 기존 패턴을 따라주세요.
```

## 중복 방지 전략

### 중복 코드 감지

```yaml
중복 감지 규칙:
  - 함수명 유사도 > 80%
  - 로직 패턴 일치 > 70%
  - import 의존성 겹침 > 60%

발견 시 액션:
  - 🚨 경고: "유사한 함수가 이미 존재합니다"
  - 💡 제안: "기존 함수를 확장하거나 래핑하세요"
  - 🔀 리팩토링: "공통 부분을 추출하세요"
```

### 자동 추출 제안

```typescript
// 중복 발견 시
if (duplicateScore > 0.7) {
  suggest({
    action: 'extract',
    location: 'shared/lib/common',
    name: 'useCommonLogic',
    benefit: '3개 컴포넌트에서 재사용 가능'
  });
}
```

## 메트릭 추적

### 재사용률 계산

```yaml
metrics:
  재사용률:
    formula: (재사용_컴포넌트 / 전체_컴포넌트) * 100
    target: 60%
    current: "${calculated}%"

  중복률:
    formula: (중복_코드_라인 / 전체_코드_라인) * 100
    target: < 5%
    current: "${calculated}%"

  패턴_일관성:
    formula: (패턴_준수_파일 / 전체_파일) * 100
    target: 95%
    current: "${calculated}%"
```

### 리포트 생성

```markdown
## 📈 재사용성 리포트

### 이번 작업
- ✅ 재사용 모듈: 3개 활용
- 💾 코드 절감: 450줄
- ⏱️ 시간 절약: 3시간

### 프로젝트 전체
- 재사용률: 62% (목표 60% 달성!)
- 중복률: 4.2% (목표 5% 이하 달성!)
- 패턴 일관성: 96% (우수)

### 개선 기회
1. `dispatch-form` 컴포넌트 → `shared/ui`로 추출 가능
2. `validate` 함수들 → `shared/validators`로 통합 가능
```

## 실행 예시

### Context7 성공 케이스
```bash
사용자: "주문 상세 모달 만들어줘"

🔍 Context7 활성화됨...
✅ 발견: OrderDetailModal이 이미 features/order/ui에 존재
✅ 발견: useOrderDetail 훅이 shared/hooks에 존재
💡 제안: 기존 컴포넌트 재사용 (95% 일치)
```

### 수동 검색 케이스
```bash
사용자: "결제 폼 컴포넌트 생성"

⚠️ Context7 사용 불가 (쿼터 초과)
🔍 수동 검색 모드...
✅ Grep으로 5개 유사 컴포넌트 발견
✅ PaymentForm 템플릿 발견 (shared/ui/forms)
💡 제안: 템플릿 확장하여 사용 (80% 일치)
```

## 사용자 오버라이드

```bash
# Context7 강제 비활성화
/major --no-context7

# 재사용 검색 건너뛰기
/major --skip-reusability

# 특정 범위만 검색
/major --reuse-scope=shared/ui
```

## 에러 처리

### Context7 실패
```
❌ Context7 연결 실패
↩️ 수동 검색 모드로 전환
🔍 Grep/Glob으로 재사용 모듈 검색 중...
```

### 토큰 한도 초과
```
⚠️ Context7 토큰 한도 도달 (2000/2000)
📊 부분 결과로 진행:
  - 컴포넌트: 80% 검색 완료
  - 유틸리티: 60% 검색 완료
```

### 재사용 모듈 없음
```
ℹ️ 재사용 가능한 모듈을 찾지 못했습니다.
✨ 새로운 모듈 생성을 시작합니다.
💡 Tip: 이 모듈을 shared/에 배치하여 재사용성을 높이세요.
```