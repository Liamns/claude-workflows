---
name: pr-review
description: GitHub PR을 조회하고 코드 리뷰를 자동으로 수행합니다. 보안, 성능, 아키텍처 준수 등을 분석하여 리뷰 결과를 터미널에 출력합니다.
---

# 🔍 PR Review Command

GitHub Pull Request를 자동으로 분석하고 코드 리뷰를 수행합니다.

## 사용법

```bash
/pr-review {PR번호}      # 특정 PR 리뷰
/pr-review current       # 현재 브랜치의 PR 리뷰
/pr-review list          # 열린 PR 목록 조회
/pr-review {PR번호} --detail  # 상세 분석 모드
```

## 프로세스

### Step 1: PR 정보 조회
```bash
# PR 기본 정보
gh pr view {PR번호} --json title,body,author,state,files

# 변경사항 조회
gh pr diff {PR번호}

# 커밋 목록
gh pr view {PR번호} --json commits
```

### Step 2: code-reviewer Agent 활성화

전문 리뷰 Agent가 다음 항목을 분석합니다:
- 🔒 보안 취약점
- ⚡ 성능 이슈
- 🏗️ 아키텍처 준수 (FSD)
- 🎯 타입 안전성
- 🧪 테스트 커버리지
- 📝 코드 품질

### Step 3: 리뷰 카테고리

#### 🔒 보안 검사
```typescript
const securityChecks = [
  'XSS 취약점',
  'SQL Injection',
  '민감 정보 노출',
  'CSRF 방어',
  '인증/인가 문제'
];
```

#### ⚡ 성능 분석
```typescript
const performanceChecks = [
  '불필요한 리렌더링',
  'N+1 쿼리 문제',
  '메모이제이션 누락',
  '번들 크기 영향',
  '비효율적인 알고리즘'
];
```

#### 🏗️ FSD 아키텍처
```typescript
const fsdChecks = [
  'Entity 순수성 위반',
  'Feature 책임 범위 초과',
  'Layer 간 잘못된 import',
  'Props drilling 발생'
];
```

### Step 4: 분석 결과 출력

## 출력 형식

### 요약 보기 (기본)
```markdown
═══════════════════════════════════════════════════════════
📋 PR #123: 운송 신청 폼 개선
═══════════════════════════════════════════════════════════

📊 변경 통계:
• 파일: 12개 변경 (+523 / -87)
• 커밋: 5개
• 작성자: @developer

🎯 리뷰 요약:
┌─────────────┬────────┬─────────────────────────────┐
│ 카테고리     │ 등급   │ 주요 발견사항                │
├─────────────┼────────┼─────────────────────────────┤
│ 🔒 보안     │ ✅ 양호 │ 이슈 없음                   │
│ ⚡ 성능     │ ⚠️ 주의 │ 2개 개선점 발견             │
│ 🏗️ 아키텍처 │ ✅ 양호 │ FSD 규칙 준수               │
│ 🎯 타입     │ ❌ 위험 │ 3개 타입 에러               │
│ 🧪 테스트   │ ⚠️ 주의 │ 테스트 누락 (커버리지 65%)  │
└─────────────┴────────┴─────────────────────────────┘

⚠️ 주요 이슈: 3개
❌ 블로커: 1개
```

### 상세 보기 (--detail)
```markdown
═══════════════════════════════════════════════════════════
📋 PR #123: 운송 신청 폼 개선 [상세 분석]
═══════════════════════════════════════════════════════════

## 🔒 보안 분석

✅ **전체 평가: 안전**

검사 항목:
• [✓] XSS 취약점 검사
• [✓] 민감 정보 노출 검사
• [✓] API 엔드포인트 보안
• [✓] 인증/인가 처리

---

## ⚡ 성능 분석

⚠️ **개선 필요 사항 발견**

### Issue 1: 불필요한 리렌더링
📍 파일: `src/features/order/ui/OrderForm.tsx`
📍 라인: 45-52

```typescript
// ❌ 문제 코드
const handleChange = (value) => {
  setFormData({ ...formData, ...value });
};

// ✅ 개선 제안
const handleChange = useCallback((value) => {
  setFormData(prev => ({ ...prev, ...value }));
}, []);
```

**영향도**: 중간
**권장사항**: useCallback으로 함수 메모이제이션

### Issue 2: 대용량 리스트 최적화 필요
📍 파일: `src/widgets/OrderList.tsx`
📍 라인: 78-92

**문제**: 100개 이상 아이템 렌더링 시 성능 저하
**해결**: React Virtual 또는 Tanstack Virtual 사용 권장

---

## 🏗️ 아키텍처 분석

✅ **FSD 규칙 준수**

검사 결과:
• [✓] Entity 레이어 순수성 유지
• [✓] Feature 책임 범위 적절
• [✓] Layer 간 의존성 올바름
• [✓] Props 전달 규칙 준수

---

## 🎯 타입 안전성

❌ **타입 에러 발견**

### Error 1: 타입 불일치
📍 파일: `src/shared/api/types.ts`
📍 라인: 23

```typescript
// ❌ 에러
interface OrderRequest {
  vehicleId: string;  // API는 number 기대
}

// ✅ 수정 필요
interface OrderRequest {
  vehicleId: number;
}
```

### Error 2: Optional 처리 누락
📍 파일: `src/entities/order/model.ts`
📍 라인: 45

```typescript
// ❌ 에러
const address = order.delivery.address;  // delivery가 optional

// ✅ 수정 필요
const address = order.delivery?.address ?? '';
```

---

## 🧪 테스트 분석

⚠️ **테스트 커버리지 부족**

현재 커버리지: 65% (목표: 80%)

누락된 테스트:
• `OrderForm.test.tsx` - 신규 기능 테스트 없음
• `VehicleSelector.test.tsx` - 파일 자체 없음
• Integration test 필요

권장 테스트 추가:
```typescript
describe('OrderForm', () => {
  it('should calculate price when vehicle selected', () => {
    // 테스트 코드
  });

  it('should validate required fields', () => {
    // 테스트 코드
  });
});
```

---

## 📝 코드 품질

### 👍 잘한 점
• 컴포넌트 분리 적절
• 일관된 네이밍 컨벤션
• 적절한 주석 작성

### 💡 개선 제안
• 복잡한 로직 함수 추출
• 에러 처리 강화
• Loading/Error 상태 UI 개선

═══════════════════════════════════════════════════════════
```

## 리뷰 등급 시스템

### 전체 등급
- **✅ LGTM** (Looks Good To Me): 즉시 머지 가능
- **⚠️ NEEDS WORK**: 개선 필요하지만 블로커 아님
- **❌ BLOCKED**: 반드시 수정 필요한 이슈 존재

### 카테고리별 등급
| 등급 | 의미 | 조치 |
|------|------|------|
| ✅ 양호 | 이슈 없음 | 조치 불필요 |
| ⚠️ 주의 | 개선점 있음 | 권장사항 검토 |
| ❌ 위험 | 심각한 문제 | 즉시 수정 필요 |

## 고급 기능

### 1. 특정 파일만 리뷰
```bash
/pr-review 123 --files "src/features/**"
```

### 2. 특정 검사만 실행
```bash
/pr-review 123 --checks security,performance
```

### 3. 커밋별 리뷰
```bash
/pr-review 123 --by-commit
```

### 4. 비교 기준 변경
```bash
/pr-review 123 --base develop  # develop 브랜치와 비교
```

## PR 목록 조회

```bash
/pr-review list

📋 열린 PR 목록:
┌─────┬──────────────────────────┬────────┬────────┬────────┐
│ #   │ 제목                      │ 작성자  │ 상태   │ 리뷰   │
├─────┼──────────────────────────┼────────┼────────┼────────┤
│ 125 │ feat: 결제 모듈 추가      │ @dev1  │ Open   │ 2 ✅   │
│ 124 │ fix: 타입 에러 수정       │ @dev2  │ Draft  │ -      │
│ 123 │ refactor: 공통 로직 추출  │ @dev3  │ Open   │ 1 ⚠️   │
└─────┴──────────────────────────┴────────┴────────┴────────┘
```

## 연동 워크플로우

### 커밋 → PR 생성 → 리뷰
```bash
# 1. 작업 완료 후 커밋
/commit

# 2. PR 생성
gh pr create

# 3. 자동 리뷰
/pr-review current
```

### Major 워크플로우와 연동
```bash
/major
→ 작업 완료
→ /commit
→ git push
→ gh pr create
→ /pr-review current
```

## 리뷰 히스토리

리뷰 기록 저장 (선택사항):
```yaml
# .claude/pr-reviews.yaml
reviews:
  - pr: 123
    date: 2025-01-06
    result: NEEDS_WORK
    issues:
      - type: performance
        count: 2
      - type: type_error
        count: 3
```

## 팀 규칙 설정

`.claude/pr-review-rules.yaml`:
```yaml
rules:
  # 자동 승인 조건
  auto_approve:
    max_lines: 50
    only_docs: true

  # 블로커 조건
  blockers:
    - security_issues
    - test_coverage < 60
    - type_errors > 0

  # 경고 조건
  warnings:
    - performance_issues > 2
    - no_tests_added
    - large_files > 1MB
```

## 문제 해결

### "PR을 찾을 수 없습니다"
```bash
# PR 목록 확인
gh pr list

# 다른 저장소 지정
gh pr view 123 --repo owner/repo
```

### "권한이 없습니다"
```bash
# GitHub CLI 인증
gh auth login
```

### "diff가 너무 큽니다"
```bash
# 파일별로 나눠서 리뷰
/pr-review 123 --files "src/features/**"
/pr-review 123 --files "src/entities/**"
```

## 통계

```markdown
📊 PR 리뷰 통계 (최근 30일)

총 리뷰: 48개
• LGTM: 28 (58%)
• NEEDS WORK: 17 (35%)
• BLOCKED: 3 (7%)

주요 이슈 유형:
• 타입 에러: 23회
• 성능 이슈: 15회
• 테스트 누락: 12회
• 보안 문제: 2회

평균 리뷰 시간: 45초
```

---

💡 **팁**: PR이 작을수록 리뷰가 빠르고 정확합니다. 큰 변경은 여러 PR로 나누세요!