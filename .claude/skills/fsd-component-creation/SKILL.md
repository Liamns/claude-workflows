---
name: fsd-component-creation
description: 팀 커스텀 FSD 아키텍처 규칙(Domain-Centric)을 준수하는 React 컴포넌트를 자동 생성합니다. Entity/Feature/Page 컴포넌트를 생성하며, 도메인 중심 구조와 타입 안전성을 자동 검증합니다. Major 워크플로우에서 사용됩니다.
allowed-tools: Read, Write, Grep, Glob, Bash(yarn type-check)
---

# FSD Component Creation Skill (Team Custom: Domain-Centric)

팀 커스텀 FSD(Feature-Sliced Design) 아키텍처 규칙에 따라 컴포넌트를 자동 생성합니다.

**팀 철학**: "One feature = one domain (like a backend service)"

## 실행 조건

다음 요청 시 자동으로 실행됩니다:
- "새 컴포넌트 생성"
- "FSD 컴포넌트 추가"
- "Entity/Feature/Page 만들어줘" (Team: Widget 레이어 제거됨)
- Major 워크플로우에서 신규 기능 추가

## 생성 프로세스

### Step 0: 기존 컴포넌트 검색 (신규 - 최우선)

컴포넌트 생성 전, 반드시 유사한 컴포넌트가 이미 존재하는지 확인합니다:

```bash
# 1. 동일 이름 컴포넌트 검색
grep -r "export.*function.*${ComponentName}" src/

# 2. 유사 컴포넌트 검색
find src/shared/ui -name "*.tsx" | xargs grep -l "similar-props"
find src/entities/*/ui -name "*.tsx"
find src/features/*/ui -name "*Form.tsx" | head -10

# 3. 재사용 가능성 평가
echo "🔍 기존 컴포넌트 검색 결과:"
echo "✅ Button 컴포넌트 - 재사용 가능"
echo "⚠️ UserForm과 70% 유사 - BaseForm 추출 고려"
```

**검색 결과 처리**:
- **100% 일치**: 기존 컴포넌트 재사용, 생성 중단
- **70-99% 유사**: 기존 컴포넌트 확장 또는 공통 부분 추출
- **70% 미만**: 새 컴포넌트 생성 진행

### Step 1: 레이어 결정 (Team Custom)

사용자 요청을 분석하여 적절한 FSD 레이어를 결정합니다:

**Entity Layer** (Optional):
- 순수한 비즈니스 엔티티 표현 (읽기 전용)
- 데이터 변환 및 포맷팅
- 상태 없는 프레젠테이션
- 인터랙션 없음 (NO buttons, forms)
- 예: Company, Vehicle (단순 데이터 표시용)
- **Team Note**: Simple projects can merge into features

**Features Layer** (Domain-Centric):
- **One feature = one domain** (백엔드 서비스와 유사)
- 관련 액션들을 도메인으로 그룹화
- 훅 기반 비즈니스 로직
- 도메인 데이터만 props로 수용
- 예: auth (sign-in + sign-up + find-password), order (create + update + cancel)

**Pages Layer** (v2.1 Pages First):
- 라우트 + 페이지 특화 로직
- Features/Entities 조합
- 페이지 특화 로직은 pages에 유지 (불필요한 feature 추출 금지)
- 예: DashboardPage, OrderListPage
- **Team Principle**: 2개 이상 페이지에서 재사용되지 않으면 pages에 유지

**Removed Layers**:
- ~~Widgets~~: Team Custom에서 제거 (features/pages로 병합)
- ~~Processes~~: FSD v2에서 deprecated

### Step 2: Slice 구조 생성

선택된 레이어에 맞는 slice 디렉토리 구조를 생성합니다:

```bash
# Entity 예시: src/entities/vehicle/
entities/vehicle/
├── api/           # (선택) API 호출 함수
├── config/        # (선택) 상수 및 설정
├── model/         # 타입, 스키마, 순수 함수
│   ├── types.ts
│   └── schema.ts
├── lib/           # (선택) 유틸리티 함수
└── ui/            # UI 컴포넌트
    ├── VehicleCard.tsx
    └── VehicleInfo.tsx

# Feature 예시 (Team: Domain-Centric): src/features/order/
features/order/
├── api/           # 도메인 관련 모든 API 호출
│   ├── createOrder.ts
│   ├── updateOrder.ts
│   ├── cancelOrder.ts
│   └── calculateFreight.ts
├── config/        # 도메인 상수
│   └── orderConstants.ts
├── model/         # 도메인 비즈니스 로직, 타입
│   ├── types.ts              # 모든 order 관련 타입
│   ├── orderSchemas.ts        # 공유 Zod 스키마
│   ├── orderValidation.ts     # 공유 검증 로직
│   ├── useOrderCreate.ts
│   ├── useOrderUpdate.ts
│   └── useOrderCancel.ts
├── lib/           # 도메인 유틸리티
│   └── orderUtils.ts
└── ui/            # 도메인 UI 컴포넌트
    ├── OrderCreateForm.tsx
    ├── OrderUpdateForm.tsx
    ├── OrderCancelButton.tsx
    └── OrderStatusBadge.tsx   # 도메인 내 공유 컴포넌트
```

### Step 3: 템플릿 적용

레이어별 템플릿을 적용하여 파일을 생성합니다.

#### Entity 템플릿

**model/types.ts**:
```typescript
// Generated by fsd-component-creation skill
export interface {EntityName} {
  id: string;
  // TODO: Add entity properties
}

export interface {EntityName}CardProps {
  data: {EntityName};
  className?: string;
}
```

**model/schema.ts**:
```typescript
// Generated by fsd-component-creation skill
import { z } from 'zod';

export const {entityName}Schema = z.object({
  id: z.string(),
  // TODO: Add schema validation
});

export type {EntityName}Input = z.infer<typeof {entityName}Schema>;
```

**ui/{EntityName}Card.tsx**:
```typescript
// Generated by fsd-component-creation skill
import type { {EntityName}CardProps } from '../model/types';

export function {EntityName}Card({ data, className }: {EntityName}CardProps) {
  return (
    <div className={className}>
      {/* TODO: Implement pure presentational UI */}
      <h3>{data.id}</h3>
    </div>
  );
}
```

#### Feature 템플릿 (Team: Domain-Centric)

**Naming**: `{Domain}{Action}` (예: OrderCreateForm, AuthSignInForm)

**model/types.ts** (도메인 내 모든 타입):
```typescript
// Generated by fsd-component-creation skill (Team: Domain-Centric)
// All types for {Domain} domain

export interface {Domain}CreateData {
  // TODO: Add create action fields
}

export interface {Domain}UpdateData {
  // TODO: Add update action fields
}

export interface {Domain} {
  id: string;
  // TODO: Add domain entity properties
}

export interface {Domain}Result {
  ok: boolean;
  data?: {Domain};
  error?: string;
}
```

**model/{domain}Schemas.ts** (도메인 공유 스키마):
```typescript
// Generated by fsd-component-creation skill (Team: Domain-Centric)
import { z } from 'zod';

// Shared schemas for {Domain} domain
export const {domain}CreateSchema = z.object({
  // TODO: Add create validation rules
});

export const {domain}UpdateSchema = z.object({
  // TODO: Add update validation rules
});

export type {Domain}CreateInput = z.infer<typeof {domain}CreateSchema>;
export type {Domain}UpdateInput = z.infer<typeof {domain}UpdateSchema>;
```

**model/{domain}Validation.ts** (도메인 공유 검증):
```typescript
// Generated by fsd-component-creation skill (Team: Domain-Centric)
// Shared validation logic for {Domain} domain

export function validate{Domain}Data(data: unknown): boolean {
  // TODO: Implement shared validation logic
  return true;
}
```

**model/use{Domain}{Action}.ts**:
```typescript
// Generated by fsd-component-creation skill (Team: Domain-Centric)
import { useState } from 'react';
import type { {Domain}{Action}Data, {Domain}Result } from './types';
import { {domain}{Action}Schema } from './{domain}Schemas';

export function use{Domain}{Action}() {
  const [isLoading, setIsLoading] = useState(false);

  const handle{Action} = async (data: {Domain}{Action}Data): Promise<{Domain}Result> => {
    try {
      // Validate
      const validated = {domain}{Action}Schema.parse(data);

      setIsLoading(true);

      // TODO: Implement business logic

      return { ok: true };
    } catch (error) {
      return { ok: false, error: String(error) };
    } finally {
      setIsLoading(false);
    }
  };

  return {
    handle{Action},
    isLoading,
  };
}
```

**ui/{Domain}{Action}Form.tsx**:
```typescript
// Generated by fsd-component-creation skill (Team: Domain-Centric)
import { use{Domain}{Action} } from '../model/use{Domain}{Action}';

interface Props {
  // TODO: Domain data props only (NO event handlers)
  // Example: userId: string; initialData?: {Domain}
}

export function {Domain}{Action}Form(props: Props) {
  const { handle{Action}, isLoading } = use{Domain}{Action}();

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    // TODO: Collect form data and call handle{Action}
  };

  return (
    <form onSubmit={handleSubmit}>
      {/* TODO: Implement form UI */}
      <button type="submit" disabled={isLoading}>
        {isLoading ? '처리중...' : '제출'}
      </button>
    </form>
  );
}
```

#### Page 템플릿 (v2.1 Pages First)

**Team Principle**: 페이지 특화 로직은 pages에 유지 (2+ 페이지에서 재사용되지 않으면 features로 추출하지 않음)

**ui/{PageName}Page.tsx**:
```typescript
// Generated by fsd-component-creation skill (Team: Pages First)
import { Header } from '@/features/header';  // 재사용 feature
import { OrderList } from '@/features/order';  // 재사용 feature

export function {PageName}Page() {
  // TODO: 페이지 특화 로직 (인증 체크, 라우트 파라미터 등)

  return (
    <div>
      <Header />
      {/* TODO: Compose Features and Entities */}
      <OrderList />
    </div>
  );
}
```

**pages/{page-name}/model/use{PageName}Data.ts** (페이지 특화 로직):
```typescript
// Generated by fsd-component-creation skill (Team: Pages First)
// Page-specific logic (not extracted to features unless reused on 2+ pages)

import { useState, useEffect } from 'react';

export function use{PageName}Data() {
  const [data, setData] = useState(null);

  useEffect(() => {
    // TODO: Page-specific data fetching
  }, []);

  return { data };
}
```

### Step 4: FSD 규칙 검증 (Team Custom)

생성된 컴포넌트가 팀 커스텀 FSD 규칙을 준수하는지 자동 검증합니다:

**Entity 검증** (Optional):
- ✅ No useState, useEffect
- ✅ No API calls (GET only if needed)
- ✅ No global state access
- ✅ Pure presentational components
- ✅ No mutations (POST/PUT/DELETE)

**Features 검증** (Domain-Centric):
- ✅ Props are domain data only
- ✅ No event handler props (onSubmit, onChange, etc.)
- ✅ Business logic in hooks
- ✅ Domain grouping: Related actions within same feature
- ✅ Type-only imports from other features (`import type { Type } from '@/features/other'`)
- ✅ Shared logic within domain (validation, schemas, types)

**Pages 검증** (v2.1 Pages First):
- ✅ Page-specific logic stays in pages
- ✅ Don't extract to features unless reused on 2+ pages
- ✅ Can have api/model/ui segments
- ✅ Composes Features and Entities

**의존성 방향 검증** (Team Custom):
```
pages → features → entities (optional) → shared
       ↓
  (type-only) features
```

**Type-Only Import 검증**:
- ✅ Feature-to-feature imports use `import type` syntax only
- ❌ Runtime imports between features (except via public API)

### Step 5: 타입 체크

생성 완료 후 자동으로 타입 체크를 실행합니다:

```bash
yarn type-check
```

타입 에러 발견 시 자동 수정을 시도합니다.

## 사용 예시

### 예시 1: Entity 생성

**사용자 요청**: "Vehicle Entity를 만들어줘. 차량 종류, 톤수, 차량번호를 포함해야 해"

**실행**:
1. Layer 결정: Entity
2. Slice 생성: `src/entities/vehicle/`
3. 파일 생성:
   - `model/types.ts` (Vehicle 인터페이스)
   - `model/schema.ts` (vehicleSchema with Zod)
   - `ui/VehicleCard.tsx` (순수 프레젠테이션)
4. FSD 규칙 검증
5. 타입 체크

### 예시 2: Feature 생성 (Team: Domain-Centric)

**사용자 요청**: "Order 도메인 Feature를 추가해줘. 주문 생성, 수정, 취소 기능이 필요해"

**실행**:
1. Layer 결정: Features (Domain-Centric)
2. Slice 생성: `src/features/order/`
3. 파일 생성:
   - `model/types.ts` (OrderCreateData, OrderUpdateData, Order, OrderResult)
   - `model/orderSchemas.ts` (orderCreateSchema, orderUpdateSchema - 공유 스키마)
   - `model/orderValidation.ts` (validateOrderData - 공유 검증)
   - `model/useOrderCreate.ts` (생성 로직 훅)
   - `model/useOrderUpdate.ts` (수정 로직 훅)
   - `model/useOrderCancel.ts` (취소 로직 훅)
   - `api/createOrder.ts` (API 호출)
   - `api/updateOrder.ts` (API 호출)
   - `api/cancelOrder.ts` (API 호출)
   - `ui/OrderCreateForm.tsx` (생성 폼)
   - `ui/OrderUpdateForm.tsx` (수정 폼)
   - `ui/OrderCancelButton.tsx` (취소 버튼)
4. FSD 규칙 검증 (Domain-Centric 구조, Props 규칙)
5. 타입 체크

### 예시 3: Page 생성 (Team: Pages First)

**사용자 요청**: "주문 목록 페이지를 만들어줘. 필터링과 정렬 기능이 필요해"

**실행**:
1. Layer 결정: Pages (v2.1 Pages First)
2. Slice 생성: `src/pages/order-list/`
3. 파일 생성:
   - `model/useOrderListData.ts` (페이지 특화 데이터 로직 - 필터링, 정렬)
   - `ui/OrderListPage.tsx` (Features 조합)
4. Pages First 검증 (페이지 특화 로직은 pages에 유지)
5. 타입 체크

## 참고 파일

상세한 레이어별 규칙과 템플릿은 다음 파일을 참조하세요:

- **templates/entity-template.md**: Entity 컴포넌트 템플릿
- **templates/feature-template.md**: Feature 컴포넌트 템플릿 (Domain-Centric)
- **templates/page-template.md**: Page 컴포넌트 템플릿 (Pages First)
- **reference/layer-rules.md**: 레이어별 상세 규칙 (Team Custom)
- **reference/props-guidelines.md**: Props 사용 가이드라인
- **architectures/frontend/fsd/config.json**: 팀 커스텀 FSD 설정
- **architectures/frontend/fsd/fsd-architecture.mdc**: 팀 커스텀 FSD 아키텍처 문서

## 보고 형식

```markdown
## ✅ FSD 컴포넌트 생성 완료

### Layer
Entity

### Slice
src/entities/vehicle/

### 생성된 파일
- model/types.ts (Vehicle, VehicleCardProps)
- model/schema.ts (vehicleSchema)
- ui/VehicleCard.tsx (순수 프레젠테이션)

### FSD 규칙 검증
- ✅ Entity 순수성 유지
- ✅ Props 규칙 준수
- ✅ 의존성 방향 정상

### 타입 체크
✅ 타입 에러 없음

### 다음 단계
1. VehicleCard 컴포넌트에 실제 UI 구현
2. 필요시 VehicleInfo 등 추가 UI 컴포넌트 생성
3. Features에서 VehicleCard 활용
```

## 주의 사항 (Team Custom)

1. **Entity는 항상 순수하게** (Optional): useState, useEffect, mutations 금지
2. **Features는 도메인 중심**: One feature = one domain, 관련 액션들을 그룹화
3. **Features Props는 도메인 데이터만**: Props에 이벤트 핸들러 전달 금지 (shared/ui 제외)
4. **Type-Only Imports**: Feature 간 참조는 `import type` 사용
5. **Pages First 원칙**: 페이지 특화 로직은 pages에 유지 (2+ 페이지에서 재사용 시 features로 추출)
6. **의존성 방향 준수**: pages → features → entities (optional) → shared
7. **Shared/UI는 예외**: 범용 UI 컴포넌트는 이벤트 핸들러 props 허용

## 에러 처리

### 에러 1: Entity에서 훅 사용 감지

```markdown
❌ FSD 규칙 위반

Entity Layer에서 useState를 사용할 수 없습니다.

**위반 코드**:
entities/vehicle/ui/VehicleCard.tsx:10
const [selected, setSelected] = useState(false);

**수정 방안**:
상태 관리가 필요하면 Feature로 이동하세요.
features/vehicle/ui/VehicleCardContainer.tsx에서 훅을 사용하고,
entities/vehicle/ui/VehicleCard.tsx는 순수 props만 받도록 수정하세요.
```

### 에러 2: Feature Props에 이벤트 핸들러 감지

```markdown
❌ FSD 규칙 위반

Features Layer Props에 이벤트 핸들러를 전달할 수 없습니다.

**위반 코드**:
features/dispatch/ui/DispatchForm.tsx
interface Props {
  onSubmit: () => void;  // ❌
}

**수정 방안**:
Props는 도메인 데이터만 받고, 이벤트 핸들러는 내부에서 정의하세요.

interface Props {
  userId: string;  // ✅
}

export function DispatchForm({ userId }: Props) {
  const handleSubmit = () => { ... };  // 내부 정의
}
```

## 통합

이 Skill은 다음과 함께 사용됩니다:
- **fsd-architect** agent: 생성 후 아키텍처 규칙 재검증
- **test-guardian** agent: Major 워크플로우에서 테스트 파일 함께 생성
- **api-designer** agent: API 관련 Feature 생성 시 API 계약 먼저 설계
